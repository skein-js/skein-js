dist/
.next/
out/
node_modules/
.nx/
pnpm-lock.yaml
LICENSE
# Generated by `nx release` — its format doesn't match Prettier, and it's tool-managed, so the
# pre-commit format:check must not gate release commits on it. Matches root + any per-package one.
CHANGELOG.md

# Generated by `nx migrate` (AI migration instructions) — tool-managed, not hand-formatted.
tools/ai-migrations/

# create-skein-js's project templates. They are the *source text* of files emitted into a scaffolded
# project, not source files of this workspace: `{{name}}` placeholders and `{{#if}}` blocks are not
# valid in any language Prettier parses. The `.tmpl` suffix already keeps them out of the eslint,
# tsc and vitest globs; this keeps them out of `nx format`.
packages/create-skein-js/templates/
