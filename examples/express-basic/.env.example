# Only needed for the `agent` graph (the Claude ReAct agent). The `echo` graph needs nothing.
# Copy to .env and fill in.

ANTHROPIC_API_KEY=sk-ant-...
# Optional — override the model used by the agent graph.
ANTHROPIC_MODEL=claude-sonnet-5
