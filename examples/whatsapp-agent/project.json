{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "example-whatsapp-agent",
  "sourceRoot": "examples/whatsapp-agent/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "examples/whatsapp-agent"
      }
    }
  }
}
