{
  "name": "@skein-js/example-migrated-langgraph",
  "version": "0.0.0",
  "private": true,
  "description": "The drop-in proof: a stock LangGraph.js project (langgraph.json + a compiled graph) run by `skein dev` in place of `langgraph dev`, unchanged.",
  "type": "module",
  "scripts": {
    "dev": "skein dev"
  },
  "dependencies": {
    "@langchain/core": "^1.2.9",
    "@langchain/langgraph": "^1.4.0"
  },
  "devDependencies": {
    "@langchain/langgraph-sdk": "^1.9.0",
    "skein-js": "workspace:*"
  }
}
