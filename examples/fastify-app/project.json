{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "example-fastify-app",
  "sourceRoot": "examples/fastify-app/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "examples/fastify-app"
      }
    }
  }
}
