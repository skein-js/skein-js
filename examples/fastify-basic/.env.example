# Only needed for the `agent` graph (the Gemini ReAct agent). The `echo` graph needs nothing.
# Copy to .env and fill in.

GOOGLE_API_KEY=AIza...
# Optional — override the model used by the agent graph.
GOOGLE_MODEL=gemini-2.5-flash
