{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "example-fastify-basic",
  "sourceRoot": "examples/fastify-basic/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "examples/fastify-basic"
      }
    }
  }
}
