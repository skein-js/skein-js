{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "server-express",
  "sourceRoot": "packages/server-express/src",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "tsup src/index.ts --format esm --dts --clean",
      "options": {
        "cwd": "packages/server-express"
      }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "options": {
        "cwd": "packages/server-express"
      }
    },
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/server-express"
      }
    }
  }
}
