{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "storage-memory",
  "sourceRoot": "packages/storage-memory/src",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "tsup src/index.ts --format esm --dts --clean",
      "options": {
        "cwd": "packages/storage-memory"
      }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "options": {
        "cwd": "packages/storage-memory"
      }
    },
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/storage-memory"
      }
    }
  }
}
