{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "server-nestjs",
  "sourceRoot": "packages/server-nestjs/src",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "tsup src/index.ts --format esm --dts --clean",
      "options": {
        "cwd": "packages/server-nestjs"
      }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "options": {
        "cwd": "packages/server-nestjs"
      }
    },
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/server-nestjs"
      }
    }
  }
}
