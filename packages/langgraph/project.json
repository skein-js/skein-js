{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "langgraph",
  "sourceRoot": "packages/langgraph/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsup src/index.ts --format esm --dts --clean",
        "cwd": "packages/langgraph"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/langgraph"
      }
    }
  }
}
