{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "telemetry-otel",
  "sourceRoot": "packages/telemetry-otel/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsup src/index.ts --format esm --dts --clean",
        "cwd": "packages/telemetry-otel"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "packages/telemetry-otel"
      }
    }
  }
}
