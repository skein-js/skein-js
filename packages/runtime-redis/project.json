{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "runtime-redis",
  "sourceRoot": "packages/runtime-redis/src",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "tsup src/index.ts --format esm --dts --clean",
      "options": {
        "cwd": "packages/runtime-redis"
      }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "options": {
        "cwd": "packages/runtime-redis"
      }
    },
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/runtime-redis"
      }
    }
  }
}
